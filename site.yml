---
- name: Install Hyprland
  hosts: localhost
  connection: local
  become: true
  become_user: sn0w
  roles:
    - yay
    - hyprland
    - nvidia
    - kitty
    - battlenet

- name: Install system packages
  hosts: localhost
  connection: local
  become: true
  roles:
    - fonts
    - zsh