---
- name: Install Hyprland
  hosts: localhost
  connection: local
  become: true
  become_user: "{{ lookup('env', 'SUDO_USER') }}"
  roles:
    - yay
    - zen-browser
    - theming
    - screenshot
    - hyprland
    - nvidia
    - kitty
    - battlenet

- name: Install system packages
  hosts: localhost
  connection: local
  become: true
  roles:
    - fonts
    - zsh
    - awww
    - bluetooth
    - audio
    - thunar
    - polkit