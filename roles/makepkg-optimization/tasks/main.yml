---
- name: Install ccache
  pacman:
    name: ccache
    state: present

- name: Set native arch CFLAGS (replace multiline original)
  replace:
    path: /etc/makepkg.conf
    regexp: '^#?CFLAGS=.*(\\\n.*)*'
    replace: 'CFLAGS="-march=native -O2 -pipe -fno-plt -fexceptions -Wp,-D_FORTIFY_SOURCE=3 -Wformat -Werror=format-security -fstack-clash-protection -fcf-protection -fno-omit-frame-pointer -mno-omit-leaf-frame-pointer"'

- name: Set CXXFLAGS to match CFLAGS
  replace:
    path: /etc/makepkg.conf
    regexp: '^#?CXXFLAGS=.*(\\\n.*)*'
    replace: 'CXXFLAGS="$CFLAGS -Wp,-D_GLIBCXX_ASSERTIONS"'

- name: Use all cores for compilation
  lineinfile:
    path: /etc/makepkg.conf
    regexp: '^#?MAKEFLAGS='
    line: 'MAKEFLAGS="-j$(nproc)"'

- name: Enable ccache in BUILDENV
  lineinfile:
    path: /etc/makepkg.conf
    regexp: '^#?BUILDENV='
    line: 'BUILDENV=(!distcc color ccache check !sign)'

- name: Use tmpfs for builds
  lineinfile:
    path: /etc/makepkg.conf
    regexp: '^#?BUILDDIR='
    line: 'BUILDDIR=/tmp/makepkg'

- name: Use multi-threaded compression
  lineinfile:
    path: /etc/makepkg.conf
    regexp: '^#?COMPRESSXZ='
    line: 'COMPRESSXZ=(xz -c -z --threads=0 -)'

- name: Use zstd for package compression
  lineinfile:
    path: /etc/makepkg.conf
    regexp: '^#?PKGEXT='
    line: "PKGEXT='.pkg.tar.zst'"
