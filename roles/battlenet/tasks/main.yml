---
- name: Install umu-launcher
  pacman:
    name: umu-launcher
    state: present

- name: Create games directory
  file:
    path: "{{ ansible_facts['user_dir'] }}/Games"
    state: directory
    mode: '0755'

- name: Download Battle.net installer
  get_url:
    url: https://www.battle.net/download/getInstallerForGame?installer=Battle.net&channel=full
    dest: "{{ ansible_facts['user_dir'] }}/Games/Battle.net-Setup.exe"
    mode: '0644'
  register: battlenet_installer

- name: Clean up installer
  file:
    path: "{{ ansible_facts['user_dir'] }}/Games/Battle.net-Setup.exe"
    state: absent
  when: battlenet_installer.changed