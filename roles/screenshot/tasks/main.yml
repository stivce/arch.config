---
- name: Install screenshot dependencies
  pacman:
    name:
      - grim
      - slurp
    state: present
  become: true
  become_user: root

- name: Install hyprquickframe from AUR
  command: yay -S --noconfirm --needed hyprquickframe
  args:
    creates: /usr/bin/hyprquickframe
